
PLAN PERFORM WaitGoalReport(){
	ID: "http://www.arbi.com//WaitGoalReport"
	PRECONDITION:
		FACT GoalReport($executor, $goalID, $result);
	BODY:
		System.out.println("goal report checked");
		RETRACT GoalReport($executor, $goalID, $result);
	UTILITY : 1000;
}